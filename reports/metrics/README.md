# Metrics Evidence

Runtime metrics are emitted through `InMemoryMetricsSink`. Export or collect these events during device runs:

- prepare start and duration
- first-frame elapsed time
- player state transitions
- late and dropped frame counters
- cache hit/miss and byte source
- network fetch duration and bytes
- proxy failures
- release latency
